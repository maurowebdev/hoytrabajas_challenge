= form_with(model: bank, class: "contents") do |form|
  - if bank.errors.any?
    #error_explanation.bg-red-50.text-red-500.px-3.py-2.font-medium.rounded-lg.mt-3
      %h2
        = pluralize(bank.errors.count, t('error'))
        = t('bank.error_messages_suffix')

      %ul
        - bank.errors.each do |error|
          %li= error.full_message

  .my-5
    = form.label :name
    = form.text_field :name, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full"

  .inline
    = form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer"
