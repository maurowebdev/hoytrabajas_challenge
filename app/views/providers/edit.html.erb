<div class="mx-auto md:w-2/3 w-full p-20 max-w-lg rounded overflow-hidden shadow-lg">
  <h1 class="font-bold text-4xl"><%= t('provider.editing') %></h1>

  <%= render "form", provider: @provider %>

  <%= link_to t('show'), @provider, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to t('provider.index'), providers_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
